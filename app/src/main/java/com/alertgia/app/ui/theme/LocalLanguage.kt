package com.alertgia.app.ui.theme

import androidx.compose.runtime.compositionLocalOf

/** Provides the current app language code ("en" or "es") to the whole composition tree. */
val LocalAppLanguage = compositionLocalOf { "es" }
